// use rust_bfs::{HasId, RepresentsSpace, BfsAbleSpace};

mod vertically_dense_space {
    mod two_d;
    // mod three_d;
}

#[cfg(test)]
mod tests {
    #[test]
    fn it_works() {
        let result = 2 + 2;
        assert_eq!(result, 4);
    }
}
